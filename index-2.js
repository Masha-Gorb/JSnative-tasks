console.log('new task')

let str = "bitcoin take over the world maybe who knows perhaps"

function shortestWord(str){
    return str.split()
}

let arr1 = str.split(' ').map(m => m.length)

console.log(Math.min.apply(null, str.split(' ').map(m => m.length) ))